<script type="text/javascript">
  /* <![CDATA[ */
  function externalLinks() {
    links = document.getElementsByTagName("a");
    for (i=0; i<links.length; i++) {
      link = links[i];
      if (link.getAttribute("href") && link.getAttribute("rel") == "external")
        link.target = "_blank";
    }
  }
  window.onload = externalLinks;
  /* ]]> */
</script>
<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Головна</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <div class='navbar-form navbar-right'>
        <span class="dropdown">
          <button class="btn btn-success" type="button" id="2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
            Інтернет-сайти
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" aria-labelledby="2">
            <li><a href="http://www2.hm.com/hu_hu/index.html" rel="external">H&M</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">...</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">....</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">....</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">.....</a></li>
          </ul>
        </span>
        <% if user_signed_in? %>
            <%= link_to 'Мої замовлення', my_order_path, method: 'get', class: 'btn btn-success' %>
            <%= link_to 'Зробити замовлення', new_order_path, method: 'get', class: 'btn btn-success' %>
            <%= link_to 'Корзина', cart_path, method: 'get', class: 'btn btn-success' %>
            <i class="glyphicon glyphicon-user white"></i> <i style="color: white; margin-right: 10px" ><%= current_user.first_name %> <%= current_user.last_name %>  </i>
            <button class="btn btn-success" type="button" id="1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
              <i class="glyphicon glyphicon-cog"></i>
            </button>
              <ul class="dropdown-menu" aria-labelledby="1">
                <li><%= link_to  edit_user_registration_path do %> <i class="glyphicon glyphicon-pencil"> </i> Редагувати профіль </li> <% end %>
                <li class="divider"></li>
                <li><%= link_to 'Вийти', destroy_user_session_path, method: 'delete', class: 'btn btn-success' %></li>
              </ul>
        <% else %>
            <div class="form-group">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <%= f.email_field :email, placeholder: 'Eлектронна пошта', class: 'form-control' %>
              <%= f.password_field :password, placeholder: 'Пароль', class: 'form-control' %>
              <%= f.submit value='Вхід', class: 'btn btn-success' %>
              <%= link_to 'Зареєструватись',  new_user_registration_path, class: 'btn btn-success' %>
            <% end %>
            </div>
        <% end %>
      </div>
    </div>
  </div><!--/.navbar-collapse -->
</nav>
